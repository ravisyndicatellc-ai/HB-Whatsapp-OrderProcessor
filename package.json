{
  "name": "whatsapp-claude-webhook",
  "version": "1.0.0",
  "description": "WhatsApp webhook that sends messages to Claude and generates invoice confirmations",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "googleapis": "^140.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
